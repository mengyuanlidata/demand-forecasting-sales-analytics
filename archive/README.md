# Legacy artifacts

`legacy_demand_forecast.csv` and `legacy_dashboard.pbix` preserve the earlier
GitHub project artifacts. They are not inputs to the current forecasting or SQL
pipeline. Use `outputs/forecasts/model_forecast_results.csv` and
`powerbi/demand_forecasting.pbip` for the current results and dashboard.
